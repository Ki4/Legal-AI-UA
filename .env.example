# Copy to apps/console/.env — never commit real values.
# The anon key is safe for the browser; the service_role key must NEVER appear
# in this repo, in frontend env files, or in any browser bundle.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
