# Session journal — Sergey, 2026-08-01

## Done

Bootstrap day, orchestrated with AI executors: monorepo (pnpm + Turborepo) with local hooks and
CI; Supabase project (EU) with the auth migration — registration → pending approval → admin
grants roles via `approve_user` RPC, verified live end-to-end; console carcass with isolated
feature tracks (services, service-detail, anatomy, account, team, design-kit) meeting only in
`routes.tsx`; design system ported into `packages/ui` (tokens, Geist, exemplar components) and
completed with AA-safe status ink tokens, the trust layer (Provenance / Confidence / Citation),
Table, EmptyState; anatomy screen renders a mock generation trace through trust components.
Docs: VISION, six ADRs, CONTRIBUTING, root + zone CLAUDE.md files, design spec.

## Decided

- Roles stay `admin | lawyer`; "main admin" is organizational, superadmin deferred (ADR-worthy
  only when a unique right appears).
- Client channel may become a chat bot: the questionnaire schema stays channel-independent
  data; chat primitives are spec'd (design spec §16) but deliberately not built.
- Status colors split into vivid (dots/fills) and `*-ink` (text) pairs after measuring
  `warn` at 3.7:1 on light (below AA); text pair now 5.9:1.

## Broken / open

- Vite dep-cache desync after dependency changes produced two React copies
  (`Invalid hook call`); fixed by clearing `apps/console/node_modules/.vite`, documented in
  README. Browser console tooling retains stale error history — verify against currently
  loaded chunks before trusting it.
- Contrast of status inks verified by calculation, not by an automated checker.

## Next

Cut issues for the parallel feature tracks (trust-layer-first priority), enable branch
protection on `main`, invite the two teammates, agree the core contract (`packages/core-client`)
and trace schema with the core owner.
