{
  "//": "`_shared/` is generated by `scripts/sync-edge-shared.mjs` and git-ignored. The edge runtime mounts this directory and nothing above it, so a path into `packages/` resolves for every tool in the repository except the one that runs the code. `pnpm functions:serve` and `pnpm functions:deploy` regenerate it first.",
  "imports": {
    "@legal-ai/law-refs": "./_shared/law-refs/index.ts",
    "@supabase/supabase-js": "npm:@supabase/supabase-js@^2.53.0"
  }
}
